<template>
  <div class="go-{{kebabCase name}}">
    <svg :width="w" :height="h">
      <rect
        x="2"
        y="2"
        :width="w - 4"
        :height="h - 4"
        fill="none"
        :stroke="colors[0]"
        stroke-width="2"
        rx="4"
      />
      <text
        :x="w / 2"
        :y="h / 2"
        text-anchor="middle"
        dominant-baseline="middle"
        :fill="textColor"
        :font-size="fontSize"
      >
        {{title}}
      </text>
    </svg>
  </div>
</template>

<script setup lang="ts">
import { PropType, toRefs } from 'vue'
import { CreateComponentType } from '@/packages/index.d'

const props = defineProps({
  chartConfig: {
    type: Object as PropType<CreateComponentType>,
    required: true
  }
})

const { w, h } = toRefs(props.chartConfig.attr)
const { colors, backgroundColor, fontSize, textColor } = toRefs(props.chartConfig.option)
</script>

<style lang="scss" scoped>
@include go('{{kebabCase name}}') {
  background-color: v-bind('backgroundColor');
}
</style>
